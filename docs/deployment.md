# Deployment Guide

## Local Deployment
1. Install dependencies: `npm install`
2. Start server: `npm start`

## Production Deployment
- Set `PORT` environment variable.
- Ensure `data/moods.json` is writable.
